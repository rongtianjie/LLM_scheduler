server:
  host: "0.0.0.0"
  port: 8001

auth:
  enabled: true

admin:
  enabled: true
  username: "admin"
  password: "admin123"
  secret_key: "llm-gateway-default-secret"

database:
  path: "data/gateway.db"

logging:
  level: "INFO"
  format: "json"  # "json" | "text"
